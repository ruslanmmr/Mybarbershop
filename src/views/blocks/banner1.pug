mixin banner()
  .banner.banner_type1
    .banner__container
      .slider.slider_dots.banner__slider
        .slide.banner-slide
          .banner-slide__background.cover-box
            img(alt="banner").lazy&attributes(attributes)
        .slide.banner-slide
          .banner-slide__background.cover-box
            img(alt="banner").lazy&attributes(attributes)
        