mixin block()
  .portfolio-block.gallery__item
    a(href="img/gallery/haircut.jpg").portfolio-block__link
      img(data-src="img/gallery/haircut.jpg", alt="").lazy
.portfolio
  .block-head
    .decorated
      span.decoration
      span.block-head__title.block-head__title_stars моё портфолио
      span.decoration
  .portfolio__container
    .container
      .slider.slider_arrows.portfolio__slider.gallery
        +block()
        +block()
        +block()
        +block()
        +block()
        +block()
        +block()
        +block()
        +block()
        +block()
        
